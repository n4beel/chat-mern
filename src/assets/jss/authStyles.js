const leftItem = {
	padding: 120,
	height: "100vh",
};

const rightItem = {
	height: "100vh",
};

const formContainer = {
	textAlign: "left",
};

const formHeading = {
	marginBottom: 60,
};

const form = {
	width: "100%",
};

const textField = {
	width: "100%",
	marginBottom: 16,
};

const button = {
	width: "100%",
	marginBottom: 60,
};

const formAction = {
	display: "flex",
	justifyContent: "space-between",
	marginBottom: 16,
};

const forgotPass = {
	padding: "9px 0",
	cursor: "pointer",
};

export const styles = {
	leftItem,
	rightItem,
	formContainer,
	formHeading,
	form,
	textField,
	button,
	formAction,
	forgotPass,
};
